<!-- Servico a ser executado -->
<div class="row">
  <div class="col-lg-8">
    <legend>Serviço a ser executado</legend>

    <% servico_escolhido = foo(locals, :servico) %>

    <% %w(buscar levar conduzir).each do |option| %>
      <div class="col-lg-12 radio-<%= option %>">
        <% if option == servico_escolhido %>
          <input type="radio" name="servico" value=<%= option %> id=<%= "service_#{option}" %> checked>
        <% else %>
          <input type="radio" name="servico" value=<%= option %> id=<%= "service_#{option}" %>>
        <% end %>

        <label for=<%= "service_#{option}" %>><%= option.capitalize %></label>
      </div>
    <% end %>
  </div>
</div>

<!-- infos de cada servico -->
<div class="row">
  <div class="col-lg-12">
    <div class="info-buscar col-lg-6">
      <span>O motorista irá ao destino buscar o passageiro de volta ao local de origem.</span>
    </div>
    <div class="info-levar col-lg-6">
      <span>O motorista irá levar o passageiro para retornar ao seu destino.</span>
    </div>
    <div class="info-conduzir col-lg-6">
      <span>O motorista irá conduzir o passageiro para o seu destino, ele aguardará até o serviço seja executado e retornará com o passageiro de volta ao local de origem.</span>
    </div>
  </div>
</div>
